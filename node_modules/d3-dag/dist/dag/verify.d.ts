/**
 * This wraps the logic to verify DAGs are valid, and is shared by the three
 * construction methods.
 *
 * @packageDocumentation
 */
import { DagNode } from "./node";
/** @internal Verify an ID is a valid ID. */
export declare function verifyId(id: string): string;
/** @internal Verify a DAG is valid. */
export declare function verifyDag<NodeType extends DagNode>(roots: NodeType[]): void;
