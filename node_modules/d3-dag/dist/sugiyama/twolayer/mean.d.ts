/**
 * The mean order operator orders the bottom layer according to the mean of
 * their parent's indices.
 *
 * @packageDocumentation
 */
import { DagNode } from "../../dag/node";
import { Operator } from ".";
export declare type MeanOperator<NodeType extends DagNode> = Operator<NodeType>;
/** Create a mean two layer ordering operator. */
export declare function mean<NodeType extends DagNode>(...args: never[]): MeanOperator<NodeType>;
